
import { useState } from 'react'
import './App.css'

function App() {

// let count = 0

const [count, setCount] = useState(0)
// const doublenum = count * 2

  return (
    <>
      {/* <h1>{count} count</h1>
      <h1>{doublenum} doublenum</h1>
      <button
      onClick={()=>{
        setCount(count + 1)
      }}
      >+1</button>

      <button
      onClick={()=>{
        setCount(prev => prev - 1)
      }}
      >-1</button> */}

      <button></button>
    </>
  )
}

export default App
